// Import the main MediCore component
import MediCore from "./MediCore.jsx";

/**
 * Root Application Component
 * Serves as the top-level container rendering the core MediCore interface.
 */
export default function App() {
  return <MediCore />;
}
